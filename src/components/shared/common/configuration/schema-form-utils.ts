import { createConfigurationDetailSchema } from '../../../features/system/configurations/detail-schema-form';

export const ConfigurationSchemaFormUtils = {
  createConfigurationDetailSchema: createConfigurationDetailSchema,
};